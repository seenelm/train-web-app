# Test info

- Name: Navigation >> should navigate to privacy page
- Location: /Users/seenelme/train-web-app/tests/navigation.test.ts:4:3

# Error details

```
Error: Timed out 5000ms waiting for expect(locator).toContainText(expected)

Locator: locator('h1')
Expected string: "Privacy Policy"
Received: <element(s) not found>
Call log:
  - expect.toContainText with timeout 5000ms
  - waiting for locator('h1')

    at /Users/seenelme/train-web-app/tests/navigation.test.ts:10:38
```

# Page snapshot

```yaml
- img "Givin Logo"
- heading "Givin" [level=2]
- heading "Tools" [level=3]
- list:
  - listitem:
    - button "Fundraising Manager"
  - listitem:
    - button "Donor Manager"
  - listitem:
    - button "Givin Assistant"
- list:
  - listitem:
    - button "Dashboard"
- button "Collapse chat"
- heading "Chat Assistant" [level=3]
- text: Hello! How can I help you today? 08:27 PM
- textbox "Type your message here..."
- button [disabled]
```

# Test source

```ts
   1 | import { test, expect } from '@playwright/test';
   2 |
   3 | test.describe('Navigation', () => {
   4 |   test('should navigate to privacy page', async ({ page }) => {
   5 |     await page.goto('/privacy');
   6 |     await page.waitForLoadState('networkidle');
   7 |     
   8 |     // Verify URL and page content
   9 |     await expect(page).toHaveURL(/.*\/privacy/);
> 10 |     await expect(page.locator('h1')).toContainText('Privacy Policy');
     |                                      ^ Error: Timed out 5000ms waiting for expect(locator).toContainText(expected)
  11 |   });
  12 |   
  13 |   test('should navigate to terms of service page', async ({ page }) => {
  14 |     await page.goto('/terms-of-service');
  15 |     await page.waitForLoadState('networkidle');
  16 |     
  17 |     // Verify URL and page content
  18 |     await expect(page).toHaveURL(/.*\/terms-of-service/);
  19 |     await expect(page.locator('h1')).toContainText('Terms of Service');
  20 |   });
  21 |   
  22 |   test('should navigate to login page', async ({ page }) => {
  23 |     await page.goto('/login');
  24 |     await page.waitForLoadState('networkidle');
  25 |     
  26 |     // Verify URL and page content
  27 |     await expect(page).toHaveURL(/.*\/login/);
  28 |     await expect(page.locator('h1')).toContainText('Welcome Back');
  29 |     
  30 |     // Verify login form elements are visible
  31 |     await expect(page.locator('[data-testid="email-input"]')).toBeVisible();
  32 |     await expect(page.locator('[data-testid="password-input"]')).toBeVisible();
  33 |   });
  34 |   
  35 |   test('should navigate to registration page', async ({ page }) => {
  36 |     await page.goto('/register');
  37 |     await page.waitForLoadState('networkidle');
  38 |     
  39 |     // Verify URL and page content
  40 |     await expect(page).toHaveURL(/.*\/register/);
  41 |     await expect(page.locator('h1')).toContainText('Create Account');
  42 |   });
  43 |   
  44 |   test('should navigate to forgot password page', async ({ page }) => {
  45 |     await page.goto('/forgot-password');
  46 |     await page.waitForLoadState('networkidle');
  47 |     
  48 |     // Verify URL and page content
  49 |     await expect(page).toHaveURL(/.*\/forgot-password/);
  50 |     await expect(page.locator('h1')).toContainText('Forgot Password');
  51 |   });
  52 | });
```